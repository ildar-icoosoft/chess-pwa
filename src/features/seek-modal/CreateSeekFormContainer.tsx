import React, { FC } from "react";
import { CreateSeekForm } from "./CreateSeekForm";

const CreateSeekFormContainer: FC<unknown> = () => {
  return <CreateSeekForm />;
};

export default CreateSeekFormContainer;
